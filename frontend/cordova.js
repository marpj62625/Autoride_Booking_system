// cordova.js stub for web — Capacitor native plugins are not available in browser.
// The app gracefully falls back to web APIs (localStorage, etc.)
window.Capacitor = window.Capacitor || { Plugins: {}, isNative: false };
